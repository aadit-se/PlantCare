# swagger-ui custom path
springdoc.swagger-ui.path=/
#test branch commit
server.port=8080

#-------------------- mysql---------------------
spring.datasource.url=jdbc:mysql://localhost:3306/gardenscheduler
spring.jpa.hibernate.ddl-auto=update
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
# spring.session.jdbc.initialize-schema:always
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

#------------------h2------------------------------
  # spring.h2.console.enabled=true
  # spring.h2.console.path=/h2
  # spring.datasource.url=jdbc:h2:mem:plantcare
  # #spring.datasource.url=jdbc:h2:file:./data/fileDb
  # spring.datasource.driverClassName=org.h2.Driver
  # spring.datasource.username=sa
  # spring.datasource.password=
  # spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
  # spring.jpa.hibernate.ddl-auto=create-drop
  # spring.jpa.show-sql=false
  # spring.jpa.properties.hibernate.format_sql=trueties.hibernate.format_sql=true

#--------.sql files
#server.servlet.context-path=/NatureOm
#spring.jpa.hibernate.ddl-auto=none
#spring.datasource.initialization-mode=always
#spring.jpa.properties.hibernate.hbm2ddl.import_files=data.sql
